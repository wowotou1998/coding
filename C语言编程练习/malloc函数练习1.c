#include <stdio.h>
#include <stdlib.h>
typedef struct{
	int data;
	int top;
} STACK;
STACK stack;
typedef STACK *p_stack;
p_stack p;
p = (p_stack)malloc(sizeof(STACK))

int main (void)
{
	void *p=0;
	int cnt=0;
	p=malloc(100*1024*1024);
	p=p+1;
	free(NULL);

	return 0;
 }

